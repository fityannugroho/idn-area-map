# URL of the application
NEXT_PUBLIC_VERCEL_URL=http://localhost:3000

# URL of the idn-area API. See https://github.com/fityannugroho/idn-area
NEXT_PUBLIC_DATA_SOURCE_URL=https://api-idnarea.fityan.tech

# URL of the idn-area boundary data/API. See https://github.com/fityannugroho/idn-area-boundary
NEXT_PUBLIC_DATA_SOURCE_BOUNDARY_URL=https://raw.githubusercontent.com/fityannugroho/idn-area-boundary/main/data

# Maximum page size for pagination
NEXT_PUBLIC_DATA_SOURCE_PAGINATION_MAX_PAGE_SIZE=100

# Default page size for pagination
NEXT_PUBLIC_DATA_SOURCE_PAGINATION_DEFAULT_PAGE_SIZE=10

# Enable Stadia tile proxy (recommended for local development to avoid strict rate limits)
# 1 = enable
NEXT_PUBLIC_STADIA_TILE_PROXY=0

# Stadia Maps API key (only used by the proxy route)
STADIA_API_KEY=

# Tile proxy cache max-age in seconds (0 = disable cache)
TILE_CACHE_MAX_AGE=86400

# Tile proxy stale-while-revalidate in seconds
TILE_CACHE_STALE_WHILE_REVALIDATE=604800
